This folder contains parameter files for the various algorithms in nbodykit. The 
files are intended to serve as examples and are also used for unit testing.

To run an example, the `NBKIT_CACHE` and `NBKIT_HOME` directories must be 
defined. From the main `nbodykit` directory, an example can be run via:


./examples/download_data
NBKIT_HOME=. NBKIT_CACHE=~/.nbodykit bin/nbkit.py FFTPower examples/power/test_pandas_hdf.params

